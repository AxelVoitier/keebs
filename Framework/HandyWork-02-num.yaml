kle:
  layers:
    Num:
      no-mod.index: 1
      shift.index: 6
      size: 4
      color: "#ff9900"

layers:
  Num:

points.zones:
  matrix.columns:
    inner:
      rows.top.layers:
        Num: "*"
      rows.top.mirror.layers:
        Num: $unset

      rows.home.layers:
        Num:
          no-mod: "."
          shift: ","
      rows.home.mirror.layers:
        Num: $unset

      rows.bottom.layers:
        Num: "/"
      rows.bottom.mirror.layers:
        Num:
          legend: Boot
          size: 2

    index:
      rows.top.layers:
        Num: 7
      rows.top.mirror.layers:
        Num:
          legend: LED_SPDU
          size: 1

      rows.home.layers:
        Num: 4
      rows.home.mirror.layers:
        Num: $unset

      rows.bottom.layers:
        Num: 1
      rows.bottom.mirror.layers:
        Num:
          legend: LED_SPDD
          size: 1

    middle:
      rows.top.layers:
        Num: 8
      rows.top.mirror.layers:
        Num:
          legend: LED_BRIU
          size: 1

      rows.home.layers:
        Num: 5
      rows.home.mirror.layers:
        Num: $unset

      rows.bottom.layers:
        Num: 2
      rows.bottom.mirror.layers:
        Num:
          legend: LED_BRID
          size: 1

    ring:
      rows.top.layers:
        Num: 9
      rows.top.mirror.layers:
        Num:
          legend: LED_NEXT
          size: 1

      rows.home.layers:
        Num: 6
      rows.home.mirror.layers:
        Num: $unset

      rows.bottom.layers:
        Num: 3
      rows.bottom.mirror.layers:
        Num:
          legend: LED_PREV
          size: 1

    pinky:
      rows.top.layers:
        Num: "-"
      rows.top.mirror.layers:
        Num:
          legend: LED_ON
          size: 1

      rows.home.layers:
        Num: 0
      rows.home.mirror.layers:
        Num:
          legend: LED_TOGGLE
          size: 1

      rows.bottom.layers:
        Num: +
      rows.bottom.mirror.layers:
        Num:
          legend: LED_OFF
          size: 1

  thumb_cluster.columns:
    outer.key:
      # layers:
      #   Num:
      mirror.layers:
        # Num:
        on_hold: Num

    under.key:
      layers:
        Num: "="
      mirror.layers:
        Num: $unset

    # inner.key:
    #   layers:
    #     Num:
    #   mirror.layers:
    #     Num:
